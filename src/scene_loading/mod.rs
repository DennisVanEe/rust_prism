pub mod transform;

// This function is the big one that loads the entire scene when given
// the path to a JSON file describing a scene:

pub fn parse_scene_file(path: &str) -> Result<()> {
    Err()
}
