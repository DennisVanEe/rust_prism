// Each extra object that wants to be loadable must register a load
// function into the following table. The load function will pass to
// it a MAP representing the JSON component depending on the type specified.

pub mod transform;